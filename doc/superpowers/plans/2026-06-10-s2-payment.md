# S2 会员与支付体系 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现再启平台的会员分级管理和微信支付对接，包含会员等级控制、订单管理、支付回调、权限拦截和月度报告次数重置。

**Architecture:** 在 S1 用户系统基础上，新增支付订单模块和会员管理模块。订单和会员状态表独立设计。权限控制通过 Spring Interceptor + 自定义注解实现。微信支付采用 v3 API，异步回调更新订单和会员状态。月度报告次数通过 Spring Scheduled 定时任务重置。

**Tech Stack:** Java 21, Spring Boot 3.2.5, MyBatis-Plus 3.5.7, MySQL 8.0, Redis 7.x, 微信支付 v3 API, Hutool 5.8.25

---

## 文件结构

```
zaiqi-api/sql/
└── V3__init_payment_schema.sql       # 新建：支付相关表

zaiqi-api/src/main/java/com/zaiqi/
├── config/
│   ├── WxPayConfig.java              # 新建：微信支付配置
│   └── WebMvcConfig.java             # 修改：注册拦截器
├── member/
│   ├── entity/
│   │   └── MemberLog.java            # 新建：会员变更日志
│   ├── mapper/
│   │   └── MemberLogMapper.java      # 新建
│   ├── service/
│   │   ├── MemberService.java        # 新建
│   │   └── impl/MemberServiceImpl.java
│   ├── controller/
│   │   └── MemberController.java     # 新建
│   └── annotation/
│       ├── RequireMemberLevel.java   # 新建
│       └── MemberLevelInterceptor.java
├── order/
│   ├── entity/
│   │   └── Order.java                # 新建
│   ├── mapper/
│   │   └── OrderMapper.java          # 新建
│   ├── service/
│   │   ├── OrderService.java
│   │   └── impl/OrderServiceImpl.java
│   ├── controller/
│   │   ├── OrderController.java
│   │   └── AdminOrderController.java
│   └── dto/
│       ├── CreateOrderRequest.java
│       ├── CreateOrderResponse.java
│       └── WxPayNotifyRequest.java
└── scheduled/
    └── ReportResetTask.java          # 新建
```

---

## 实施任务

### Task 1: 支付与订单数据库 Schema

- [ ] **Step 1: 创建 zaiqi-api/sql/V3__init_payment_schema.sql**

```sql
-- 再启平台 V3: 支付与订单表

CREATE TABLE IF NOT EXISTS `zq_order` (
    `id` BIGINT NOT NULL,
    `order_no` VARCHAR(64) NOT NULL COMMENT '订单号',
    `user_id` BIGINT NOT NULL,
    `product_type` TINYINT NOT NULL COMMENT '1-月度会员 2-季度会员 3-年度会员 4-AI分析 5-决策报告',
    `product_name` VARCHAR(100) NOT NULL,
    `amount` INT NOT NULL COMMENT '支付金额（分）',
    `currency` VARCHAR(3) DEFAULT 'CNY',
    `status` TINYINT DEFAULT 0 COMMENT '0-待支付 1-已支付 2-已退款 3-已关闭',
    `wx_transaction_id` VARCHAR(64) DEFAULT NULL,
    `wx_notify_raw` TEXT DEFAULT NULL,
    `paid_time` DATETIME DEFAULT NULL,
    `refund_time` DATETIME DEFAULT NULL,
    `close_time` DATETIME DEFAULT NULL,
    `remark` VARCHAR(500) DEFAULT NULL,
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    `deleted` TINYINT DEFAULT 0,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_order_no` (`order_no`),
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='支付订单表';

CREATE TABLE IF NOT EXISTS `zq_member_log` (
    `id` BIGINT NOT NULL,
    `user_id` BIGINT NOT NULL,
    `old_level` TINYINT DEFAULT 0,
    `new_level` TINYINT NOT NULL,
    `old_expire_time` DATETIME DEFAULT NULL,
    `new_expire_time` DATETIME DEFAULT NULL,
    `change_type` TINYINT NOT NULL COMMENT '1-开通 2-续费 3-禁用 4-到期 5-后台调整',
    `operator_id` BIGINT DEFAULT NULL,
    `order_id` BIGINT DEFAULT NULL,
    `remark` VARCHAR(200) DEFAULT NULL,
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    INDEX `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='会员变更日志表';
```

- [ ] **Step 2: Commit**

```bash
git add zaiqi-api/sql/V3__init_payment_schema.sql && git commit -m "feat: 添加支付和会员日志数据库脚本"
```

---

### Task 2: 微信支付配置

- [ ] **Step 1: 创建 WxPayConfig.java**

```java
package com.zaiqi.config;

import lombok.Data;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.Configuration;

@Data
@Configuration
@ConfigurationProperties(prefix = "wx.pay")
public class WxPayConfig {
    private String appId;
    private String mchId;
    private String apiV3Key;
    private String privateKeyPath;
    private String serialNo;
    private String notifyUrl;
    private String refundNotifyUrl;
}
```

- [ ] **Step 2: 在 application.yml 末尾追加**

```yaml
wx:
  pay:
    app-id: ${WX_APP_ID:wx-mock-appid}
    mch-id: ${WX_MCH_ID:1234567890}
    api-v3-key: ${WX_API_V3_KEY:mock-v3-key-must-be-32-chars-long}
    private-key-path: ${WX_PRIVATE_KEY_PATH:/data/certs/apiclient_key.pem}
    serial-no: ${WX_SERIAL_NO:mock-serial-no}
    notify-url: https://api.zaiqi.com/api/pay/notify
    refund-notify-url: https://api.zaiqi.com/api/pay/refund-notify
```

- [ ] **Step 3: 编译验证后 Commit**

---

### Task 3: 实体与 Mapper

Order 实体 + OrderMapper + MemberLog 实体 + MemberLogMapper

- [ ] **Step 1: 创建 Order.java / OrderMapper.java / MemberLog.java / MemberLogMapper.java**
- [ ] **Step 2: 编译验证后 Commit**

---

### Task 4: 订单创建与微信支付统一下单

**商品配置：**
| productType | 商品 | 金额(分) |
|:----------:|------|:-------:|
| 1 | 月度高级会员 | 6900 |
| 2 | 季度高级会员 | 18900 |
| 3 | 年度高级会员 | 28900 |
| 4 | AI 单次分析 | 990 |
| 5 | 决策报告超额 | 990 |

- [ ] **Step 1: 创建 CreateOrderRequest/CreateOrderResponse DTO**
- [ ] **Step 2: 创建 OrderService + OrderServiceImpl**

核心方法：
- `createOrder(userId, request)`：生成订单号 → 插入订单 → 调用微信预支付 → 返回 prepay_id 和签名参数
- `handlePaymentSuccess(orderNo, wxTransactionId, notifyRaw)`：更新订单状态 → 调用 MemberService.handleProductPurchase

- [ ] **Step 3: 创建 OrderController**（POST `/api/order/create` + GET `/api/order/{orderNo}`）
- [ ] **Step 4: 编译验证后 Commit**

---

### Task 5: 支付回调处理

- [ ] **Step 1: 创建 PayNotifyController**（POST `/api/pay/notify`）
- [ ] **Step 2: 编译验证后 Commit**

---

### Task 6: 会员等级管理服务

**MemberService 接口方法：**
- `handleProductPurchase()` - 支付成功后处理会员升级/AI购买
- `getMemberStatus()` - 查询会员状态
- `hasPermission()` - 检查权限（chat/post/like/comment/report_full）
- `decrementReportCount()` - 报告次数减1
- `disableMember()` - 后台禁用（status=0）
- `enableMember()` - 后台启用

- [ ] **Step 1: 创建 MemberService + MemberServiceImpl**
- [ ] **Step 2: 创建 MemberController**（GET `/api/member/status` + GET `/api/member/prices`）
- [ ] **Step 3: 编译验证后 Commit**

---

### Task 7: 会员权限注解与拦截器

- [ ] **Step 1: 创建 @RequireMemberLevel 注解和 MemberLevelInterceptor 拦截器**
- [ ] **Step 2: 修改 WebMvcConfig 注册拦截器**
- [ ] **Step 3: 编译验证后 Commit**

---

### Task 8: 月度报告次数调度任务

- [ ] **Step 1: 创建 ReportResetTask**（每小时检查 + 每天凌晨检查会员到期）
- [ ] **Step 2: 编译验证后 Commit**

---

### Task 9: 管理后台订单与会员接口

- [ ] **Step 1: 创建 AdminOrderController**（GET 订单列表 + PUT 禁用/启用）
- [ ] **Step 2: 编译验证后 Commit**

---

## 自检清单

| Spec 需求 | 对应 Task |
|-----------|----------|
| 三档会员定价 | Task 4 - PRODUCTS |
| AI 单次/报告超额 ¥9.9 | Task 4 - PRODUCTS |
| 微信支付统一下单 | Task 4 - createOrder |
| 支付回调通知 | Task 5 - PayNotifyController |
| 会员到期时间累积 | Task 6 - calculateExpireTime |
| 会员权限拦截 | Task 7 - MemberLevelInterceptor |
| 月度 5 次报告重置 | Task 8 - ReportResetTask |
| 会员到期自动降级 | Task 8 - checkVipExpiry |
| 管理后台订单/会员 | Task 9 - AdminOrderController |
